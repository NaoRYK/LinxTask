module.exports = {
    // Domain settings
    domain: 'taskinx.com',
  
    // Environment variables
    env: {
      NODE_ENV: 'production',
    },
  
    // Build settings
    build: {
      outputDir: 'dist',
    },
  };